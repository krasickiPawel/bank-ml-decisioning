{
  "model_name": "sentence-transformers/all-MiniLM-L6-v2",
  "dim": 384,
  "chunks": [
    {
      "chunk_id": "01_overview.md::chunk000",
      "source": "rag_docs/01_overview.md",
      "text": "# Credit Risk Decisioning — Overview\n\nThis repo is a demo of an end-to-end credit risk decisioning service:\n- a training script that logs models and evaluation to MLflow,\n- an inference API (FastAPI) that loads a selected model (by best_model.json / run id / registry),\n- a simple UI (Streamlit) for demo scenarios and monitoring,\n- lightweight monitoring signals (request logging + drift metrics),\n- a minimal RAG module that answers questions using project docs and artifacts.\n\nThe core output of the model is a probability score:\n- `score = P(default = \"bad\")` for a given customer feature vector.\nDecisioning applies a single rule:\n- **decline if score >= threshold**, otherwise approve."
    },
    {
      "chunk_id": "01_overview.md::chunk001",
      "source": "rag_docs/01_overview.md",
      "text": "iven customer feature vector.\nDecisioning applies a single rule:\n- **decline if score >= threshold**, otherwise approve.\n\nThis demo is intentionally pragmatic:\n- it avoids heavy dependencies,\n- focuses on reproducible MLflow logging,\n- keeps the API stable even when optional pieces are missing (old runs, missing artifacts),\n- provides clear demo flows and “what you see is what you logged”.\n\n## Components\n\n### Training (`src/train.py`)\n- Loads the German Credit dataset (OpenML `credit-g`).\n- Splits data into train/val/test.\n- Trains a model pipeline (preprocessing + classifier).\n- Selects the best decision threshold on validation data.\n- Logs:\n  - model artifact (`model/`),\n  - threshold metrics,\n  - plots,\n  - schema artifacts (`expected_columns.json`, example payload),\n  - drift reference file (`drift_reference_train.csv`) when enabled."
    },
    {
      "chunk_id": "01_overview.md::chunk002",
      "source": "rag_docs/01_overview.md",
      "text": "tifacts (`expected_columns.json`, example payload),\n  - drift reference file (`drift_reference_train.csv`) when enabled.\n\n### API (`src/api/app.py`)\n- Loads the model bundle on startup and serves:\n  - `/health` for operational info,\n  - `/schema` with expected columns and example payload,\n  - `/predict` returning score/decision and (when available) reasons,\n  - `/reload` to re-load the currently configured “best” model without restarting the process,\n  - `/metrics/drift` to compute drift vs the reference file.\n\n### UI (`streamlit_app.py`)\n- Preset scenarios (SAFE / BORDER / RISKY).\n- Calls the API endpoints and shows results.\n- Drift view uses request logs as “current” population.\n- RAG tab answers questions using local docs (and later MLflow artifacts)."
    },
    {
      "chunk_id": "01_overview.md::chunk003",
      "source": "rag_docs/01_overview.md",
      "text": "ew uses request logs as “current” population.\n- RAG tab answers questions using local docs (and later MLflow artifacts).\n\n## What is “RAG” here?\nRAG = Retrieval Augmented Generation:\n- Retrieve relevant chunks from local documents / artifacts.\n- Compose an answer based on retrieved context.\nIn MVP mode we can be “extractive” (no LLM), or we can generate with an LLM later.\nThe key point: answers come with citations (source + snippet), so it’s explainable."
    },
    {
      "chunk_id": "02_mlflow_howto.md::chunk000",
      "source": "rag_docs/02_mlflow_howto.md",
      "text": "# MLflow — How to use it in this demo\n\nThis project uses MLflow for:\n- storing runs (params, metrics),\n- storing artifacts (plots, CSVs, schema files),\n- storing the trained model pipeline.\n\n## Tracking server\nThe API expects an MLflow tracking server, typically:\n- `MLFLOW_TRACKING_URI=http://localhost:5050`\n\nYou can open the UI in a browser:\n- http://localhost:5050\n\n## How runs are selected\nThe API loads a model using one of these strategies (highest priority wins):\n\n1) Explicit model registry (if configured):\n   - `MODEL_NAME=...` and optionally `MODEL_STAGE=Production` or alias.\n   - This is the “production style” setup.\n\n2) Explicit run id:\n   - `MODEL_RUN_ID=...`\n   - Useful for pinning a specific run."
    },
    {
      "chunk_id": "02_mlflow_howto.md::chunk001",
      "source": "rag_docs/02_mlflow_howto.md",
      "text": "is is the “production style” setup.\n\n2) Explicit run id:\n   - `MODEL_RUN_ID=...`\n   - Useful for pinning a specific run.\n\n3) `best_model.json` in the repo:\n   - generated by `src/scripts/select_best_model.py`\n   - This is the most demo-friendly approach:\n     you can train multiple runs and pick a winner without touching registry.\n\n4) Fallback:\n   - latest runnable run in the experiment (if implemented in the loader).\n\n## Finding a run in the MLflow UI by run id\nMLflow UI is not great at “search by run_id” directly from the runs table.\nThe practical workaround:\n- Build the URL with experiment id + run id:\n  `http://localhost:5050/#/experiments/{experiment_id}/runs/{run_id}`\n\nYou can get experiment id for a run in code:\n- `experiment_id = client.get_run(run_id).info.experiment_id`\n\nThe API `/health` can expose a direct `run_url` so you can click it from Streamlit."
    },
    {
      "chunk_id": "02_mlflow_howto.md::chunk002",
      "source": "rag_docs/02_mlflow_howto.md",
      "text": "get_run(run_id).info.experiment_id`\n\nThe API `/health` can expose a direct `run_url` so you can click it from Streamlit.\n\n## Artifacts this demo relies on\nSome endpoints/features depend on these artifacts:\n\n- `expected_columns.json`\n  Used by `/schema` to tell clients what input columns exist.\n\n- `example_payload.json` (optional)\n  Nice for demo; pre-fills a request payload.\n\n- `drift_reference_train.csv`\n  Used by `/metrics/drift` as the baseline distribution.\n  If a run was trained before this was logged, drift will return a friendly error.\n\n- `feature_importance_top.csv`, `perm_importance_top30.csv`, `threshold_cost_table_val.csv`\n  Not required for core inference. Great for demo explanations and RAG."
    },
    {
      "chunk_id": "02_mlflow_howto.md::chunk003",
      "source": "rag_docs/02_mlflow_howto.md",
      "text": "tance_top30.csv`, `threshold_cost_table_val.csv`\n  Not required for core inference. Great for demo explanations and RAG.\n\n## Registry vs best_model.json\nRegistry is closer to enterprise workflows (promotion, approvals, gating).\nbest_model.json is faster for demo and experiments, because:\n- you can update selection logic in one script,\n- switching models is a file update + `/reload`,\n- no UI clicking required."
    },
    {
      "chunk_id": "03_decision_policy.md::chunk000",
      "source": "rag_docs/03_decision_policy.md",
      "text": "# Decision policy and outputs\n\n## What the model predicts\nThe model outputs:\n- `score = P(default = bad)`\n\nSo higher score means higher risk.\n\n## Decision rule\nThis demo applies a single rule:\n- **decline if score >= threshold**\n- otherwise approve\n\nThreshold is selected on validation data, using a cost function.\n\n## Cost-based thresholding\nThreshold is chosen to minimize:\n- `cost = fp_cost * FP + fn_cost * FN`\n\nWhere:\n- FP = predicted bad but actually good (false decline),\n- FN = predicted good but actually bad (false approve).\n\nDefaults in this project often reflect that FN is more expensive than FP.\n\n## “Reasons” (explainability)\nReason codes depend on the classifier type:\n\n- LogisticRegression:\n  - we can compute local contributions as `x_i * coef_i`\n  - these are on the transformed feature space (after preprocessing / one-hot)\n  - stable, fast, and good for demos."
    },
    {
      "chunk_id": "03_decision_policy.md::chunk001",
      "source": "rag_docs/03_decision_policy.md",
      "text": "_i`\n  - these are on the transformed feature space (after preprocessing / one-hot)\n  - stable, fast, and good for demos.\n\n- XGBoost:\n  - we can request per-feature contributions using `pred_contribs=True`\n  - also on transformed feature space\n  - works without the `shap` dependency.\n\n- RandomForest / HistGradientBoosting:\n  - true local explanations are not available in a lightweight way without extra tooling\n  - for demo we typically show:\n    - global feature importance artifacts,\n    - and keep per-request reasons empty,\n    - or return “top global features” as a weak substitute.\n\nIn other words:\n- if `reasons=[]`, it does not mean the model is broken.\n- it usually means “this model type doesn’t support cheap local attributions”."
    },
    {
      "chunk_id": "03_decision_policy.md::chunk002",
      "source": "rag_docs/03_decision_policy.md",
      "text": "]`, it does not mean the model is broken.\n- it usually means “this model type doesn’t support cheap local attributions”.\n\n## Monitoring and drift\nWe log requests (anonymized/limited to features needed for scoring).\nDrift compares:\n- reference distribution (training baseline, artifact `drift_reference_train.csv`)\nvs\n- current distribution (last N requests from `data/requests.jsonl`)\n\nWe compute:\n- PSI for numeric and categorical (bucketed / top categories),\n- KS for numeric (when enough data exists).\nThese are heuristics, not a replacement for full monitoring, but good for demos."
    }
  ]
}